;(function($){

	// $(".left-nav-list li").eq(0).addClass("active");
	var swiperTopics = new Swiper('.topics-swiper', {
	    direction: 'vertical',
	    freeMode: true,
	    mousewheelControl: true,
	    slidesPerView: 'auto',
	    scrollbar: '.topics-scrollbar',
	});

	// $(".topics-scrollbar").show();
	
})(jQuery);